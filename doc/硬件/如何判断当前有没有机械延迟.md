# 如何判断当前有没有机械延迟

> 背景：直线巡线时"跑着跑着往外扭得很厉害"，怀疑机械部分响应慢——传感器信息来得快，但电机/机构启动、换向需要时间，导致每次修正都冲过头。本文给出**怎么判断到底有没有机械延迟、有多大**的一套方法。

## 一、先理解：症状为什么"像"机械延迟

控制回路里只要存在**死区时间/滞后（dead time / lag）**，就会产生相位滞后 → 过冲 → 大幅左右摆的极限环振荡。当前现象正好对得上：

- 直行 108/108 稳（**稳态**没问题）
- 一旦需要**转向修正**就冲过头、甩出去（**瞬态**响应差）

问题只出在"需要改变运动状态"的瞬间，这正是延迟/惯性藏身的地方。所以怀疑方向合理。

## 二、关键前提：现在有**两层延迟叠在一起**，必须先拆开

判断机械延迟之前，务必意识到还有一层**软件延迟**混在里面，否则会把软件的锅算到机械头上：

| 延迟来源 | 说明 | 数据佐证 |
|---|---|---|
| **软件延迟** `slew_rate=600` | 代码里故意限速，PWM 每毫秒最多变 0.6 单位 | log14 中 mode3 目标内轮 37，实测 26ms 才从 108 降到 91——这段迟钝一大半是 slew 造成的，不是电机 |
| **机械延迟**（待判定） | 电机惯量、减速箱回程间隙(backlash)、静摩擦(stiction)、尤其**轮子反转**（+前进→-反转要先减速停再反向） | log14 中"已找回线(模式变0)、左轮却还在 -42 继续猛掰把车甩过头"——slew 和机械惯量都会造成，光看 log 分不出主因 |

**结论：不先摘掉软件延迟，就无法判断机械延迟。** 下面实验按此设计。

## 三、判断步骤（按顺序做，逐层定位）

### 实验 A：摘掉软件延迟，看过冲是否还在 【零成本，先做这个】
- **操作**：`slewrate` 调到很大（如 5000）或临时不限速，其它参数不动，重跑同一段直线。
- **判读**：
  - 过冲/"往外扭"**基本消失** → 主因是 **slew（软件）**，机械没那么糟，回去调软件参数即可。
  - 过冲**依旧严重** → **机械延迟是主因**，怀疑成立，继续做 B/C。
- 意义：5 分钟、零成本，直接判定"软件 vs 机械"占比，是整套判断的分水岭。

### 实验 B：摘掉"反转"这个最重的机械动作
- **操作**：`sharpratio`、`xsharpratio` 设为 ≥0（只减速、不反转），找线逻辑也改成不反转，重跑。
- **判读**："往外扭"明显缓解 → 说明坑你的主要是**轮子反转的机械延迟**（换向要克服反向惯量，延迟最大）。这基本等于确诊机械延迟的一种具体形态。

### 实验 C：直接量机械延迟 【最实在的实锤】
做**阶跃响应测试**，量出具体 ms 数：
- **启动延迟**：停止 → 突然给 base PWM，测轮子"多久开始转"和"多久到稳速"。
- **换向延迟**（最关键）：前进 base → 突然给 -base，测反向要多少 ms 才真正反转。
- **测量手段**：
  - 有编码器：在固件里给"下发 PWM 时刻"和"编码器检测到转动/到目标转速时刻"各打时间戳，相减即延迟。
  - 无编码器：手机高速摄像（240fps）拍轮子/贴个标记点，数帧算时间（每帧 ≈4.2ms）。
- **判读参考**：
  - 启动/换向延迟只有几十 ms → 机械不是主要瓶颈。
  - 上百 ms（尤其换向）→ 机械延迟确实大，需从机械/控制策略上治（见第四节）。

### 实验 D：降速旁证
- **操作**：`speed` 17 → 12，重跑。
- **判读**：降速后过冲明显变小 → 说明是**动量/惯量**在作怪（速度越高动量越大、越难及时刹住），与机械延迟同源。

### 附：低 PWM 静摩擦(stiction)单独一测
- 从 0 开始每次 +5 慢慢加 PWM，记录轮子**刚开始转**的那个 PWM 值。
- 若这个"起转 PWM"明显大于 0（比如要到 40~60 才动），说明有明显静摩擦死区——小修正指令根本推不动轮子，也会表现成"反应慢/修正无效"。

## 四、若确认机械延迟大，正确的应对方向（避免踩坑）

⚠️ 有实质延迟时**不能靠加大增益/加猛修正去追**——那样只会更振荡。正确做法：

1. **避免轮子反转**：反转延迟最大，直线巡线尽量只"内轮减速不反转"，去掉 sharp/xsharp 的负比例。
2. **降速 + 适当减小修正力度**，给机械留反应时间。
3. **传感器前探（最对症的机械补偿）**：传感器阵列往车头前方移，机械慢就用更靠前的传感器提前"看到"偏差，等于加了预读距离/时间，用空间换时间补偿延迟——常比调参数更管用。
4. **用 PID 微分项(Kd)做预测**：Kd 对偏差变化率响应，能提前刹住、抵消部分滞后（已配 kd=5）。但延迟大时 Kd 有限度，前三条更根本。
5. **加编码器做闭环速度控制**：既能压左右轮不一致（见"传动比"方向），又能减小电机响应延迟和 stiction 影响。

## 五、执行建议

**先做实验 A**（把 slew 调大重跑）——零成本、5 分钟、直接判定机械 vs 软件占比。A 的结果决定后续重心：
- A 说明是软件为主 → 回到软件调参（见 `调试记录.md` 方向 1）。
- A 说明是机械为主 → 做 B/C 量化，然后按第四节从机械/控制策略上治，并推进编码器方案（`调试记录.md` 方向 2）。

> **实验 A 已实测（log15/log16）**：slew 从 600 提到 5000 后直线不再丢（`log15分析.md`），换 PID 后直线走得笔直、err 全程 ±0.33 内（`log16分析.md`）。**结论：直线场景下软件延迟(slew=600)是主因，机械延迟不是瓶颈。**

## 六、关于"减速箱慢一拍 / 人家直驱变化快"的分析

用户对比别人的车：别人轮子变速非常快、没有减速箱（"无极变速"），自己的轮子明显慢一拍。这个观察物理上成立，但要和上面的实测结论合起来看。

### 1. 减速箱确实让轮子换速更慢（三个机理）
- **反射惯量**：电机转子惯量折算到轮端要 **×减速比²**（如 30:1 → 放大 900 倍），加/减速更费劲，天然慢一拍；直驱没有这个放大。
- **回程间隙(backlash)**：齿轮有旷量，**换向瞬间**要先"吃掉"旷量轮子才真正反转——这是最明显的一拍延迟，直驱无齿轮就没有。
- **齿轮/静摩擦(stiction)**：小 PWM 可能推不动，要到一定量才"啪"地动，也表现为迟钝。

### 2. 但直线场景，减速箱不是瓶颈（已被数据证明）
- log16（PID）证明：**就用现在带减速箱的车，直线能走得笔直**。
- 之前"往外扭"是**软件 slew ~200ms 延迟叠在机械慢一拍之上**，两者合力才甩出去；去掉软件那 200ms + 用 PID 连续小修正后，剩下的机械慢一拍**已小到不影响直线**。
- 所以：**为直线去换硬件没必要。**

### 3. 减速箱真正会咬人的地方：高速 + 急弯/发卡
- 直线：修正量小、不换向 → 反射惯量和 backlash 几乎不影响。
- 急弯/发卡：需要**又快又大**的速度变化、尤其**内轮反转** → 同时踩中反射惯量 + backlash 两个雷，这里会明显吃亏。**换硬件的收益主要在弯道/高速，不在直线。** 建议先把弯道测起来，看减速箱卡不卡得住需要的半径和速度，再决定值不值得动硬件。

### 4. 纠正一个说法："无极变速"
**所有 PWM 直流电机都是无级调速的**（指令上没有"档"）。别人轮子"变化快"不是因为"无极 vs 有极"，而是**响应更快**——低惯量电机（空心杯/无刷）+ 小减速比 + 可能更高的控制频率。真正的杠杆是"**响应速度**"（电机惯量 + backlash + 控制率），不是"有没有档位"。

### 5. 低成本先量化 / 缓解（不急着换硬件）
- **测 backlash**：电机断电，用手正反拧轮子，能空转的那点角度就是旷量，越大换向越迟钝。
- **测换向延迟**：240fps 拍轮子，前进 base→突然 -base，数帧算 ms（本文实验 C）。有了 ms 数才能跟"人家的车"客观对比。
- **弯道不换硬件也能缓解**：少用反转、传感器往前探（用空间换时间）、Kd 做预测。
- **要换硬件**：空心杯/无刷直驱确实快，但代价是**扭矩小、低速控制难、易打滑、要配不同驱动**，是换设计点而非纯升级；竞速车常这么干，但也普遍配 PID + 高控制率。

## 七、齿轮比选择（当前 114.7:1，可选 38.2:1 / 12.7:1）

背景：当前减速比 **114.7:1**；手上还有 **38.2:1** 和 **12.7:1**。最早用 38.2:1 时"总是冲出去"，但**那时是 5 个传感器**，现在是 **8 个传感器 + PID + slew 已修**。问：能不能改回 38.2:1，或直接上 12.7:1？

### 1. 减速比换算（相对当前 114.7:1）
| 减速比 | 相对速度 | 反射惯量(∝比²) | 相对扭矩 | 特点 |
|---|---|---|---|---|
| **114.7:1**（当前） | 1× | 1× | 1× | 最慢、最稳、扭矩大，但"慢一拍"最明显、极限速度低 |
| **38.2:1** | **~3×** | **~1/9** | ~1/3 | 折中：响应快很多、速度上来、扭矩仍够用 |
| **12.7:1** | **~9×** | **~1/81** | ~1/9 | 最快、响应最snappy，但扭矩最小、对控制/传感器要求最高 |

**降低减速比正好治你说的"慢一拍"**：反射惯量按 **比²** 下降——换到 38.2 惯量降到约 1/9，换到 12.7 降到约 1/81，轮子换速会明显变快，且级数少通常 backlash 也更小、换向更利索。

### 2. 关键判断：之前 38.2:1 "冲出去"不是齿轮比的错
那次失败的真实原因，是我们这几天已经修掉的一堆问题：**只有 5 个传感器（位置分辨率粗）+ bang-bang 离散档 + 软件 slew ~200ms 延迟 + 没上 PID**。速度快只是**把这些控制缺陷放大、让它更早冲出去**，齿轮比是"暴露问题"不是"制造问题"。现在 8 传感器 + PID + slew 已放开，控制回路强了很多，**38.2:1 值得、也应该重新试。**

### 3. 建议：逐级降，一次只变一个变量
不建议直接跳到 12.7:1。按这个顺序最稳、也最能看清是谁的功劳：

1. **先换 38.2:1**（等于在"已修好"的条件下重测当年翻车的那个比）。若现在稳 → 直接证明当年是控制的锅，不是齿轮。
2. 38.2 稳了、还想更快 → 再上 **12.7:1**。
3. 每换一次比，**其它参数尽量别动**，先跑直线再跑弯道，对比 log。

### 4. ⚠️ 换比后必须同步调的东西
- **降 `speed` 设定值**：换低比后，**同样的 PWM = 快得多的车速**。12.7:1 在 speed 17 下可能快到根本控不住。换比后先把 speed 调低（比如 12.7:1 先从 speed 5~8 起步），确认能控再往上加。
- **重测/微调 PID**：车速变了，同样的 kp/kd 表现会变（更快的车需要更早、更强的修正，或更大的 Kd 阻尼）。直线先复测，再进弯道。
- **看扭矩够不够**：12.7:1 扭矩只有 1/9，注意起步会不会带不动、急弯内轮反转会不会因为动量大而"刹不住/掉不了头"、爬坡或地毯阻力大时会不会打滑/堵转。
- **弯道是试金石**：直线快没意义，重点看低比+高速下急弯/发卡能不能跟住——这正是减速箱 backlash 和速度共同作用的地方。

### 5. 小结
- 你想解决"慢一拍"，**降减速比是对症的**（惯量按比²降）。
- 当年 38.2:1 翻车主要是**控制不行**（5传感器+bang-bang+软件延迟），现在条件变了，**先回 38.2:1 重测**是最合理的一步；稳了再考虑 12.7:1。
- 换比一定要**同步降 speed、重调 PID、盯扭矩、测弯道**，一次只变一个变量。

---
相关文档：`调试记录.md`、`log分析/log16分析.md`、`log分析/log15分析.md`、`log分析/log14分析.md`、`log分析/log13分析.md`、`BANGBANG算法修改方案.md`
