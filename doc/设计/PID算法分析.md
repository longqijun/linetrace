# PID巡线算法详细分析

**分析基于的代码版本（commit）**：`9db3f99cccb1abda65a39092d3573f8f4dc1d76a`

本文汇总PID模式（`TRACK_ALGO_PID`）相关的实现细节、实测数据分析、以及"车身晃动"问题的排查过程，便于后续调参和排障时查阅，不用每次从头翻聊天记录。

---

## 1. 实现细节（track_module.cpp）

### 1.1 误差来源

`error = sensor_position_from(is_white)`（sensor_module.cpp:40-50）：
- 5路传感器（CH2~CH6）按是否压白线做二值判断，加权算出位置
- 归一化到 **-1.00 ~ +1.00，步进0.25，只有9档离散值**（不是连续量）
- 正=线偏右（需要向右修正：左轮加速/右轮减速）

这是理解PID表现的关键前提：**误差信号本质是阶梯状的离散量，不是平滑连续信号**。

### 1.2 PID计算（track_module.cpp，2026-07-29重写后）

```
output = Kp*error + Ki*integral + Kd*filtered_derivative
pwm_l  = base + lroundf(output)   （clamp到±255）
pwm_r  = base - lroundf(output)
```

**2026-07-29重写，原因和改动点**：原实现`dt`直接用`track_update()`两次调用的真实间隔，而`track_update()`跟着`loop()`裸奔（实测约1ms一圈），等于在1ms尺度上对着一个只有9档离散值的误差信号求导——用户实测`Kd=0`（纯P）依然晃得厉害，证明问题不止是Kd数值大小，根子是"loop多快就重算一次PID、多快就换一次电机PWM"这个节奏本身对传感器噪声过于敏感。重写后：

- **PID固定周期重算**（`PID_INTERVAL_MS=10`），跟`loop()`实际速度解耦：`track_update()`每圈都读传感器，但只有距上次PID计算≥10ms才重新算一次`output`，中间的loop沿用`_pid_hold_output`（不再跟着loop()频率抖）。`dt`因此变成稳定的10ms左右，不再是~1ms的噪声放大器
- **微分项加一阶低通滤波**（`PID_DERIV_FILTER_ALPHA=0.3`）：`_pid_filtered_deriv += α*(raw_derivative - _pid_filtered_deriv)`，把离散误差跳档瞬间产生的微分尖峰摊到后面几个周期，不再单点炸出满幅打摆
- 积分限幅`PID_INTEGRAL_CLAMP=±2.0`不变，防止小误差长期累积失控
- 输出改成`lroundf()`四舍五入，不再是向零截断——原来的截断会把小误差直接吃成0，制造数值死区
- `pid_reset()`同步清空`_pid_filtered_deriv`/`_pid_hold_output`；调用时机不变（切换算法、`track_set(on/off)`、进入丢线状态）

旧版本（`dt`实时值+无滤波微分+`(int)`截断）的具体行为见下方第3节的log4实测分析，仅作历史记录，**结论不再代表当前代码的实际行为**，重新调参前建议先按新实现重新观察。

### 1.3 与bang-bang共用的异常状态判定

丢线（`lost`）和十字路口/宽线（`cross`）两个判定两种算法共用，不受`_algo`影响：
- 丢线：延续`_last_dir`方向找线，1.5s超时停车
- 十字路口：直行穿过，不触发转向，不更新`_last_dir`

只有"五路有白线、不丢线、不是十字路口"这个正常跟踪分支，才按算法分岔。

### 1.4 默认增益

```
PID_KP_DEFAULT = 40.0
PID_KI_DEFAULT = 0.0
PID_KD_DEFAULT = 5.0
```
均为起调参考值，未实车验证过（代码注释里写明）。`pid kp/ki/kd N`可在线调，`save`持久化到`/config.json`。

---

## 2. loop()/PID更新频率

`sensor_debug.ino`的`loop()`**没有任何`delay()`**（物理按钮消抖那一次性30ms不算，正常巡线不会走到）。每圈依次：`bt_connected()`检查 → `cmd_poll()` → `track_update()`（内含5路`analogRead()`，sensor_module.cpp:5,13-16，12位分辨率）→ 每200ms额外打印一次调试信息。

- ESP32单次`analogRead()`典型耗时约100~200微秒，5路合计约0.5~1ms，其余部分是微秒级浮点运算和GPIO写操作
- **粗估loop()一圈约1ms左右，几百到上千Hz**（这是根据ESP32 `analogRead`一般耗时推算的估计值）——**2026-07-29已加实测埋点**（`sensor_debug.ino`），`micros()`记录每圈耗时，每秒打印一次`LOOP xxx/s avg=xxxus min=xxxus max=xxxus`（受`print`开关控制，默认不输出，开`print usb/bt/file on`才能看到）

**2026-07-29实测重大发现（log-2.txt）**：用户专门抓了一份log验证loop计时精度，结果发现**loop每隔约1.6秒左右就会被卡住3.5~4秒**（`min`只有12~140us，但同一秒的`max`能到400万微秒），track on之后（BANGBANG, speed=16）这个卡顿仍在继续，导致那段时间平均每圈要36~37ms。根因不是loop代码本身慢，是当时`print_module.cpp`的flash环形log实现用`LittleFS.open(path,"r+")+seek()`往一个256KB大文件的任意偏移量写数据，而LittleFS这类日志结构文件系统对大文件随机偏移写非常慢（需要遍历内部块链表定位），每次flush（约每10行log）都触发一次3~4秒的卡顿——**相当于巡线过程中电机指令每隔几秒被冻结3~4秒，这比"晃"严重得多，是判断画龙/抖动问题时必须先排除的一个干扰变量**。已重写为多段文件轮转（`LOG_SEGMENT_COUNT=4`，纯顺序追加，不再seek），详见`print_module.h`/`.cpp`注释。**在这次修复之前采集的所有log（包括log4.txt和之前几次实车测试）都可能受这个多秒卡顿影响**，建议修复后重新采集一次新log，确认loop和PID计算是否恢复到微秒级的正常水平，再继续判断晃动的真实原因。
- **PID计算和`motor_set()`下发是跟loop()同频率执行的，不受30ms调试打印节流限制**——`track_module.cpp:211`那个30ms只节流"打印log"这一件事，PID本身每圈都在算、每圈都在下发新PWM

这个高频率是后面"传感器阈值抖动"假说的重要背景：loop远快于机械响应速度，如果车身正好压在两个传感器阈值交界处，`analogRead`噪声可能让`is_white[]`高频来回跳变，`error`和PWM也跟着高频抖动。

---

## 3. log4.txt实测分析（Kp=40, Ki=0, Kd=5，旧版实现，仅历史记录）

完整报告见`log分析/log4-分析报告.md`，核心结论摘要：

1. **比例项工作正常但增益偏小**：稳态数据与`output=40×error`完全吻合，但误差打满时修正量只有±40，仅占base(102)的~39%、PWM满量程的~16%，转向权威不足
2. **丢线极其频繁**：526条记录里LOST_R/LOST_L占30%，规律是误差打满(E:±1.00)后车辆几乎立刻丢线——车速相对弯道曲率而言，PID修正跟不上
3. **微分项在放大噪声**：日志中4次瞬时`L:-255 R:255`满幅尖峰。根因是`error`是离散阶梯值，Kd对着阶梯信号求导，一旦噪声导致误差跳档，`derivative`被放大到远超实际趋势的量级，Kd=5进一步放大到饱和
4. 结论：当时是"披着PID外壳的弱P控制器"，Ki=0没有积分作用，Kd=5对离散量化误差弊大于利，Kp又明显偏弱

---

## 4. "车身晃动"排查过程（截至2026-07-29的历史记录；4.1~4.3是重写前的分析，重写后需要重新实测）

### 4.1 第一轮：怀疑Kd（已验证，不是唯一原因）

基于log4分析，最初判断满幅打摆主要由Kd导致，建议用户先把`pid kd 0`（保持Kp=40, Ki=0）测试。

**用户实测反馈**：`Kp=40, Ki=0, Kd=0`（纯P控制），**车身仍然晃得厉害**。

→ **这推翻了"Kd是唯一原因"的假设**，纯P控制下依然晃动，说明还有别的机制在起作用。

### 4.2 第二轮：两个候选假设

1. **转向权威不足+车速偏快导致的"画龙"（hunting/overshoot）**：修正量最大只有±40（占base的~39%），车速较快时车身冲过中心线后修正才生效，来回小幅度过冲，表现为持续的S形摆动
2. **传感器阈值抖动导致的高频正负反跳**：赛道解析.txt提到赛道板材"亮面护膜反光性强"，车身压在两个传感器阈值交界处时`is_white[]`可能高频跳变，配合第2节提到的loop高频特性，`error`和PWM会跟着高频抖动，表现为原地小范围高频哆嗦

这两种在log里的特征完全不同（1是`E:`缓慢正负漂移+PWM缓慢摆动；2是`E:`短时间内高频正负跳变），**目前没有新log数据，无法下定论**。

### 4.3 待执行的诊断步骤

- **抓Kd=0状态下的log**：BT当前不稳定，改用`print file on`（不需要全程在线连接）→ 实车跑`track on`→`track off`→ 事后USB连接执行`log dump`取出`/track.log`内容
- **行为描述排查**（log拿到之前的临时手段）：
  - 晃动在直道还是弯道/过渡段更明显？
  - 晃动样子：缓慢画龙蛇形，还是原地高频哆嗦？
  - 电机有无快速正反切换的"哒哒哒"声？
  - 当前车速档位（`speed`命令查）？
  - **对照实验（最有效，不需要log工具）**：切回`algo bangbang`跑同一段赛道是否也晃。若bang-bang不晃、只有PID晃 → 问题在PID逻辑本身；若两种算法都晃 → 更可能是车速/机械/传感器阈值层面问题，跟PID三个参数关系不大

---

## 5. 后续调参方向（重写后需要重新实测，以下是重写前的分析）

- 若确认是"转向权威不足"：提高Kp（比如到100+），让打满误差时修正量占更大比例——这条跟第2节的重写无关，重写只解决了"计算节奏放大噪声"，没有改变Kp的转向权威够不够，仍然需要实车验证
- 若确认是"传感器阈值抖动"：第2节的重写（固定周期+微分低通滤波）已经从PID计算这一层面缓解了"loop高频+离散信号"放大噪声的问题，**建议先用重写后的代码重新测一遍晃动是否改善，再决定要不要继续动传感器层面（滤波/死区/换加权模拟量）**
- Kd现在跑的是滤波后的微分，含义已经和重写前不一样，之前"Kd=0还晃"的实测结论不能直接套用到新实现上，需要重新从0开始试

### 5.1 重写后待验证清单

- [ ] 实车确认固定周期(`PID_INTERVAL_MS=10`)+微分低通滤波(`PID_DERIV_FILTER_ALPHA=0.3`)后，`Kp=40,Ki=0,Kd=0`是否还晃
- [ ] 如果不晃了，再逐步加回Kd，观察滤波后的微分是否还会导致满幅打摆
- [ ] 如果依然晃，说明第4.2节假设1（转向权威不足+车速偏快的画龙过冲）权重更大，按原计划提高Kp
- [ ] `PID_INTERVAL_MS`和`PID_DERIV_FILTER_ALPHA`目前是编译期常量，未做成运行时命令，如果需要现场试不同周期/滤波系数，需要另外加`pid interval N`之类的命令

---

## 6. 风险与待办（与设计说明.md一致，不重复展开）

默认增益（Kp=40/Ki=0/Kd=5）和积分限幅（±2.0）均未经实车验证；Kd项对传感器阈值抖动敏感；loop真实频率是估算值未实测；尚未与bang-bang在同一赛道上做过量化对比。
