# 8路模拟量PID算法：可调参数与调节方法

**背景**：2026-07-29把PID的误差来源从二值判断（`sensor_position_from`，离散阶梯值）
换成了模拟量加权（`sensor_position_analog_from`，见`sensor_module.cpp`），本质原因是
用户观察到"传感器不是一下子从2000跳到600，是2000→1900→1700慢慢变到600的"——模拟量
加权直接把这个连续变化过程用上，不再靠离散判断制造人为的阶跃。

本文只讲**这条模拟量PID链路上有哪些能调的变量、怎么调**，不重复讲PID公式本身的原理
（见`PID算法分析.md`）或8路传感器的物理布局（见`8路传感器方案.md`）。

---

## 1. 参数总览

按信号从"传感器读数"到"电机PWM"流经的顺序排列：

| 层级 | 参数 | 命令 | 默认值 | 能否运行时调 | 作用 |
|---|---|---|---|---|---|
| 1.传感器标定 | `white_ref[CH]` | `whiteref CH VALUE` | CH2~CH6实测值，CH1/CH7/CH8占位600 | ✅ | 每路"压线中心"的ADC参考值，决定权重计算的分母 |
| 1.传感器标定 | `black_ref[CH]` | `blackref CH VALUE` | CH2~CH6实测值，CH1/CH7/CH8占位2400 | ✅ | 每路"完全没压线"的ADC参考值 |
| 2.丢线判定 | 权重和阈值（代码里写死`0.3f`） | 无命令，需改代码 | 0.3 | ❌ | 8路权重加起来低于这个值就判定丢线(NAN)，交给外层`lost`逻辑处理 |
| 3.PID核心增益 | `pid_kp` | `pid kp N` | 40.0（当前测试用的是100） | ✅ | 比例增益，误差→修正量的线性系数 |
| 3.PID核心增益 | `pid_ki` | `pid ki N` | 0.0 | ✅ | 积分增益，消除持续性稳态偏差用 |
| 3.PID核心增益 | `pid_kd` | `pid kd N` | 0.0（代码默认5.0，但当前config里是0） | ✅ | 微分增益，压制过冲/震荡用 |
| 3.PID内部节奏 | `PID_INTERVAL_MS`（代码常量） | 无命令，需改代码 | 10ms | ❌ | PID重算周期，跟loop()实际速度解耦 |
| 3.PID内部节奏 | `PID_DERIV_FILTER_ALPHA`（代码常量） | 无命令，需改代码 | 0.3 | ❌ | 微分项一阶低通滤波系数，越小滤波越强 |
| 3.积分限幅 | `PID_INTEGRAL_CLAMP`（代码常量） | 无命令，需改代码 | ±2.0 | ❌ | 防止积分长期累积失控 |
| 4.输出平滑 | `slew_rate` | `slewrate N` | 800（当前测试用的是5000） | ✅ | 下发PWM每秒最多变化多少单位 |
| 5.基础车速 | `speed` | `speed N` | 12（当前测试用的是16） | ✅ | base PWM档位，PID的修正量是加/减在这个值上的 |

标"❌"的几个是编译期常量，想改需要我帮你改代码重新烧录，不是现在这套命令行能直接调的——如果实测下来发现需要调这几个，告诉我，我可以帮你开放成命令。

---

## 2. 详细调节方法（建议按这个顺序，一次只改一个变量）

### 第0步：先确认传感器标定是准的（比调PID优先级更高）

**为什么排第一**：模拟量PID的误差信号完全依赖`white_ref`/`black_ref`算出来的权重，标定不准，后面Kp调得再仔细也是在拿错误的输入调参，等于白费。你现在的CH1/CH7/CH8还是占位值（`white_ref`600/`black_ref`2400），没有实车标定过。

**怎么标定**：
1. `print on`，把车放到赛道白线上，观察8路原始ADC值（`[count] ...`那行括号里的数字）
2. 让每一路轮流压在白线正中央，记下此时该路的ADC值 → 这就是`whiteref CH 记下的值`
3. 让每一路完全离开白线（压在黑色背景上），记下ADC值 → 这就是`blackref CH 记下的值`
4. CH2~CH6理论上不用重新标（已经是实测值），但建议顺手核对一下现在的实车数值跟`white_ref`/`black_ref`里存的是否还对得上（传感器安装高度、赛道反光情况变了的话，参考值也可能要跟着变）

**怎么验证标定是否合格**：手动缓慢把车从赛道一侧推到另一侧，同时看`apos:`这个值——应该跟着平滑地从-1滑到+1（或反过来），不应该有突然跳变或者中途卡在某个值不动。如果`apos:`大部分时间显示0.00（不动），说明权重和长期低于0.3的丢线阈值，大概率是标定错了（比如`white_ref`比实际白线读数还大，导致权重公式算出负值被砍成0）。

### 第1步：纯P（Ki=0, Kd=0），从小往大试，找震荡边界

1. `pid ki 0`、`pid kd 0`，从`pid kp 40`开始（这是历史上验证过的起点）
2. 在直道/缓弯上跑，逐步加大Kp（比如40→60→80→100...），每次加完跑一遍
3. 你已经实测过`Kp=40`太弱（反复丢线，见`log6分析.md`）、`Kp=100`太强（剧烈画龙）——**说明能用的Kp大概率在40~100之间**，建议接下来试60、70这种中间值，用类似二分的方式往中间收拢
4. 判断标准：**这一档Kp在直道上还画龙吗？在最急的弯（发卡弯）还能不能跟上、会不会直接冲出去？** 两个条件都满足，说明这个Kp在你能接受的范围内；只满足前者说明还能再往上加一点，只满足后者说明得往下调

**这一步要留意的架构限制**：`PID算法分析.md`第4节提到过，纯线性P公式要让内轮真正反转（应对发卡弯），Kp必须大于base(102)。如果试到Kp接近100还没到反转门槛就已经开始震荡，说明这套线性公式在这个赛道的发卡弯前可能存在天花板——这种情况下不建议继续死磕Kp数值，回来告诉我，可以考虑参照bang-bang的做法给PID也加一个"极端误差时直接反转"的非线性区间。

### 第2步：加一点Kd，压住过冲

找到第1步"能接受但还有一点点画龙倾向"的Kp后，尝试加一点Kd（比如从1开始，`pid kd 1`），不要一步到位设大值。Kd的作用是让车在快速逼近中心线时提前减速，减少冲过头再反向修正的幅度。

**这次模拟量改造后，Kd应该比以前更好调**：以前误差是离散阶梯值，微分在跳档瞬间会炸出尖峰（`PID算法分析.md`第2节详细分析过这个问题），现在误差是连续量，微分算出来的"变化率"更贴近车身真实的运动趋势，加上`PID_DERIV_FILTER_ALPHA=0.3`的低通滤波，双重加持下Kd导致的异常尖峰应该明显减少。如果加了Kd之后又开始抖，退回到0，说明这一路的信号还没干净到能用微分，先回去检查第0步的标定质量。

### 第3步：Ki，一般不需要

除非实测发现车身在直道上有**持续性**的单向偏移（比如一直往左偏一点点，不是随机抖动），否则`pid ki 0`保持不变。这条赛道没有已知的持续性系统偏差，积分项加了大概率只是徒增复杂度和积分饱和风险。

### 第4步：slew_rate，收尾用

前三步都调好之后，如果PWM输出还有一点不必要的跳跃感（不是来自误差信号本身，而是纯粹的PWM过渡生硬），再来调`slewrate`。**注意当前用的是5000（接近不限速）**，这次崩的这次测试没有靠限速兜底，如果第1~3步调完还有轻微摇摆，可以把`slewrate`往下调（比如从5000降到1500、1000），但不要一开始就指望靠限速掩盖Kp本身没调好的问题——上次的经验是`slewrate`调太小反而会让车来不及应对真正的急弯，顺序上应该是先把Kp/Kd调到本身就不怎么震荡，slew_rate只用来收尾磨平剩余的小毛刺。

### 第5步：确认速度档位下Kp是否要跟着变

`pid_kp`算出来的修正量是**加在base上的绝对值**，不是按base的比例算的。如果之后想把`speed`从16往上调（跑更快），同样的Kp在更高的base下，修正量占比会变小，相当于转向权威变相变弱，可能需要把Kp适当调大一点配合。反过来调低速度，也可能需要把Kp调小，避免相对修正力度过猛。**每次改了`speed`，建议把第1步重新走一遍，不要假设之前调好的Kp在新速度下还适用。**

---

## 3. 常见问题排查

| 现象 | 大概率原因 | 怎么查 |
|---|---|---|
| `apos:`一直是0.00不动 | 权重和长期低于0.3丢线阈值，标定错了 | 核对`white_ref`是不是比实际压线时的ADC值还大（应该更小，白色是低值） |
| `apos:`只在很小范围内变化（比如-0.2~+0.2），压满线也到不了±1 | `white_ref`/`black_ref`区间设太宽，实际压线时算出的权重到不了1.0 | 重新标定，白参考值要贴近实际压线时的最低ADC值，不要留太多余量 |
| Kp加大后一直画龙，退回小Kp也丢线严重 | 可能是第1节提到的线性公式天花板问题，或者标定本身有误差被Kp放大 | 先返回第0步核对标定，标定confirmed没问题的话，把这个现象反馈给我讨论加非线性急弯区间 |
| 明明标定过了，PID还是不稳 | 别忘了`file_log`是否开着——开着`print file on`会拖慢控制环，之前bang-bang冲出去和这个有直接关系（见`log5分析.md`） | 确认当前config的`file_log`是`false`（你这次贴的config正好是false，排除了这个变量） |

---

## 4. 风险与待办

- 本文档第2节的调参顺序是常规PID调参思路+这个项目已踩过的坑，**具体数值（Kp在哪个区间、Kd从多少开始）都还没有实车验证过**，需要实测
- 编译期常量（`PID_INTERVAL_MS`/`PID_DERIV_FILTER_ALPHA`/`PID_INTEGRAL_CLAMP`/丢线权重阈值0.3）目前都没开放成运行时命令，如果调参过程中发现需要动这几个，需要改代码重新烧录，不能像`pid kp`这些一样直接在线试
- CH1/CH7/CH8的`white_ref`/`black_ref`仍是占位值，本文档第0步的标定必须先做，否则后面的Kp/Kd调参没有意义
- 还没有拿这套模拟量PID跟之前的二值PID、以及bang-bang做过量化对比，"模拟量是不是真的比二值好调"目前只是理论推断，需要实测验证
